# Public site address (used for sitemap, canonical URLs and Open Graph)
NEXT_PUBLIC_SITE_URL=https://www.example.com
# Optional: a form endpoint (for example a Formspree URL). If empty, the contact form opens WhatsApp with the message ready to send.
NEXT_PUBLIC_FORM_ENDPOINT=
